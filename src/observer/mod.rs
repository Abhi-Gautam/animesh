//! `animesh doctor` — the EXPLAIN of animesh.
//!
//! Read-only, no-network, sub-10ms surface that reports DB path, schema
//! version, item counts, cache health, last sync, rate-limit headroom,
//! binary version.

// Implementation lands in T25.
